# Change request PRC-114

Finance wants two things before the next billing cycle:

1. Raise the `team` tier from $49 to $59 per month.
2. Bill overage seats **per day used** instead of a flat monthly seat charge.

Item 2 needs a schema change: `subscriptions` has no per-day usage anywhere.

Nobody has said what happens to accounts already mid-cycle.
